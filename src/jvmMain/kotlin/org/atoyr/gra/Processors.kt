package org.atoyr.gra

import java.awt.image.BufferedImage

internal fun createBufferedImageProcessor(image: BufferedImage): ImageProcessor {
    TODO()
}

internal fun createVectorImageProcessor(vectorData: MutableList<PaintOperationWrapper>): ImageProcessor {
    TODO()
}